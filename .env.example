# Anthropic API key — used server-side only, never exposed to the frontend
ANTHROPIC_API_KEY=sk-ant-...

# Password for the /admin page (HTTP Basic Auth; username can be anything)
ADMIN_PASSWORD=change-me

# Optional
PORT=3000
# DB_PATH=/var/data/assessments.db
